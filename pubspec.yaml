name: carousell_sample
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Manejos de estados
  flutter_bloc: 7.0.1
  equatable: 2.0.3
  # Plugins
  video_player: 2.1.12
  # Repositorios
  media_repository:
    path: packages/media_repository
  user_repository:
    path: packages/user_repository

dev_dependencies:
  flutter_lints: 1.0.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
